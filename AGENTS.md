# Agent instructions

Read `README.md` before changing this project. Keep the extension's shell-command acceptance criteria deliberately narrow; reject unsupported commands rather than broadening the parser without focused tests.

Run `npm test` after code changes. Do not commit secrets, Pi credentials, or session data.
